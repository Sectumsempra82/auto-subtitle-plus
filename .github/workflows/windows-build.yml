name: Windows x64 build

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: windows-build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v6

      - name: Set up Python 3.13
        uses: actions/setup-python@v6
        with:
          python-version: '3.13'
          cache: pip
          cache-dependency-path: requirements.txt

      - name: Install test dependencies
        shell: pwsh
        run: python -m pip install --upgrade pip; python -m pip install -r requirements.txt; python -m pip install srt jiwer sacrebleu==2.6.0 faster-whisper; python -m pip install --editable . --no-deps

      - name: Run regression tests
        shell: pwsh
        env:
          QT_QPA_FONTDIR: C:\Windows\Fonts
          QT_QPA_PLATFORM: offscreen
        run: |
          $ciTemp = Join-Path $env:GITHUB_WORKSPACE '.ci-temp'
          New-Item -ItemType Directory -Force -Path $ciTemp | Out-Null
          $env:TEMP = $ciTemp
          $env:TMP = $ciTemp
          python -m unittest discover -s tests -q 2>&1 | Tee-Object -FilePath regression-tests.log
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Upload regression log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: regression-log-${{ github.sha }}
          path: regression-tests.log
          if-no-files-found: warn
          retention-days: 14

      - name: Build the single Windows x64 EXE
        shell: pwsh
        run: .\packaging\Build-Windows.ps1 -PythonExe (Get-Command python).Source -Output dist/windows-light -SkipTests

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: AutoSubtitlePlus-Windows-x64-${{ github.sha }}
          path: |
            dist/windows-light/AutoSubtitlePlus-Windows-x64.exe
            dist/windows-light/AutoSubtitlePlus-Windows-x64.exe.sha256
          if-no-files-found: error
          retention-days: 14

      - name: Publish tagged release assets
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          # All tags so far are release candidates (vX.Y.Z-rc.N); mark them
          # as pre-releases until a non-rc tag is cut.
          prerelease: ${{ contains(github.ref_name, '-rc.') }}
          generate_release_notes: true
          files: |
            dist/windows-light/AutoSubtitlePlus-Windows-x64.exe
            dist/windows-light/AutoSubtitlePlus-Windows-x64.exe.sha256

      - name: Publish rolling main release assets
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: continuous
          name: Continuous Windows build
          prerelease: true
          make_latest: false
          generate_release_notes: false
          files: |
            dist/windows-light/AutoSubtitlePlus-Windows-x64.exe
            dist/windows-light/AutoSubtitlePlus-Windows-x64.exe.sha256
